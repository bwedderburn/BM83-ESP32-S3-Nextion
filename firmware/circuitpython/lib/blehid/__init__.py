# blehid package: minimal BLE HID Consumer Control wrapper for the
# ESP32-S3 + BM83 receiver. Exposes only volume/mute media keys; bonding
# management lives entirely on the host phone (forget on iOS/Android side
# and power-cycle the ESP32 to re-pair).
