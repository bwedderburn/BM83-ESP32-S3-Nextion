[pytest]
# No --maxfail here: CI should report every failure in one run.
# Use `pytest -x` locally when you want fail-fast behavior.
addopts = --tb=short --disable-warnings
pythonpath =
    firmware/circuitpython
    firmware/circuitpython/lib

markers =
    mpy: tests that require optional mpy-cross build lane
